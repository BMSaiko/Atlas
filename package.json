{
  "name": "atlas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --import ./test/_register-loader.mjs --test test/*.test.mjs",
    "dev:token": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "devDependencies": {
    "@axe-core/cli": "^4.13.0",
    "@lhci/cli": "^0.14.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vite": "^6.4.3"
  }
}
